import { Canvas } from "@react-three/fiber";
import ThreeLayout from "./ThreeLayout";

const Character = () => {
  return (
    <Canvas>
      <ThreeLayout />
    </Canvas>
  );
};

export default Character;
